<Window x:Class="StockAnalyzer.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StockAnalyzer.Views"
        mc:Ignorable="d"
        WindowState="Maximized"
        DataContext="{Binding MainViewModel, Source={StaticResource Locator}}"
        Title="MainWindow">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Button Content="解析"  VerticalAlignment="Center" HorizontalAlignment="Center" Width="200" Height="50" Command="{Binding AnalyzeCommand}" />
        <TextBlock Grid.Row="1" Text="{Binding PickedStockDataList.Count,Mode=OneWay}" />
        <DataGrid
            Grid.Row="2"
            ItemsSource="{Binding PickedStockDataList}">
        </DataGrid>
    </Grid>
</Window>
